import 'package:flutter/material.dart';

class SizedBoxesHieghtTen extends StatelessWidget {
  const SizedBoxesHieghtTen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 10,
    );
  }
}
